<pages:WpfUIPlatformPage
    x:Class="WpfUIDemoApp.Views.Pages.GridViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:pages="clr-namespace:WpfUIDemoApp.Views.Pages"
    mc:Ignorable="d"
    Title="GridViewPage" Height="450" Width="800">

    <Page.Resources>
        <CollectionViewSource
            x:Key="GroupExtensions"
            Source="{Binding Extensions}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Category" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Page.Resources>

    <DataGrid
        SelectionMode="Single"
        AutoGenerateColumns="False"
        CanUserResizeRows="False"
        CanUserSortColumns="False"
        CanUserReorderColumns="False"
        ItemsSource="{Binding Source={StaticResource GroupExtensions}}">

        <DataGrid.GroupStyle>
            <GroupStyle>
                <GroupStyle.ContainerStyle>
                    <Style TargetType="{x:Type GroupItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type GroupItem}">
                                    <Grid>
                                        <Expander IsExpanded="True">
                                            <Expander.Header>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock
                                                        Margin="10"
                                                        FontWeight="Bold"
                                                        Text="{Binding Name, 
                                                            StringFormat=Category {0}}" />
                                                </StackPanel>
                                            </Expander.Header>
                                            <ItemsPresenter />
                                        </Expander>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </GroupStyle.ContainerStyle>
            </GroupStyle>
        </DataGrid.GroupStyle>

        <DataGrid.Columns>
            <DataGridTemplateColumn
                Width="70"
                MinWidth="70"
                MaxWidth="70"
                Header="1" />

            <DataGridTextColumn
                Binding="{Binding Name}" Header="2" />

            <DataGridTextColumn
                Binding="{Binding Description}" Header="2" />
        </DataGrid.Columns>
    </DataGrid>
</pages:WpfUIPlatformPage>